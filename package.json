{
  "name": "digipology-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck && tsc -p scripts"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
