import { PlaceholderPage } from "../components/PlaceholderPage";

export function CreatePage() {
  return (
    <PlaceholderPage
      eyebrow="Creator studio"
      title="Create a game"
      description="The creator workspace will turn components, assets, and optional Lua behavior into a game you can share."
    />
  );
}
